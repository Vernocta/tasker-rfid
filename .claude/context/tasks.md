# Tasks

## NOW

1. **Prepare the Beontag visit** — the free RF test. Ask them to bring a handheld reader and test on real product.
   - Confirm the date in writing (16th vs 17th conflict)
   - Measure a box label, get the printer model
   - Photograph pallets (wrapped + unwrapped), boxes, contents
   - Have ready that morning: one full box of each family, one wrapped pallet, one unwrapped, the printer
   - Ask for a reader/antenna integrator referral

## NEXT

2. **Demo seed command** — register containers with real SKUs, stock them, build a pallet, dispatch it. For showing the team, Cocchiarella, and Beontag something real instead of zeros.
3. **Fresh-machine smoke test** — clone, up, migrate, seed, simulate, confirm on the dashboard. Catches anything that only works because the dev laptop has something installed.
4. **Real seed data** — replace placeholder SKUs and customers.

## LATER

5. Step 10 — real reader integration
6. Neon cloud database
7. Dashboard iteration based on watching someone actually use it
8. Serve the dashboard to a dock screen (`API_BASE_URL` must be reachable from that machine)

## BLOCKED

- `mode` and `tag_class` — pending RF test
- Hardware purchase — pending RF test results and supplier referral
